# Lower bounds rather than exact pins, so installing this project does not
# downgrade packages in a shared environment. Pin exactly in a deployment
# lockfile, not in a demo's requirements file.
fastapi>=0.115
uvicorn>=0.34
pydantic>=2.10
scikit-learn>=1.6
numpy>=2.0
pytest>=8.3
httpx>=0.28
